import { CodeWriter } from "../src/codeWriter"

describe("Initial state", () => {
    test("When there is no filename", () => {
        // Given
        // When
        const writer = new CodeWriter();
        // Then
    })
})