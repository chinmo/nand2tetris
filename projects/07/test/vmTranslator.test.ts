import { vmTranslator } from "../src/vmTranslator";
describe("File Handling", () => {
  test("No Arguments", () => {
    // Given
    // When
    // Then
    expect(vmTranslator("")).toBeUndefined();
  });
});
